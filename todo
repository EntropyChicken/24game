when there is no battle master, people have a button to become the battle master (a "host")
    so instead of having no button, there's just a different button: to become host
    must make sure that nobody can become a second host if for whatever reason they have an old button
    should check for hosts when you click such a button

when the player gets a doubler, they should see bright energetic flashes on their own screen in fact, each doubler should have its own associated color, and it should also be how it is displayed on the battle master's screen.
    negative: bright hot pink (like the text color)
    non-integer: bright yellow (like the text color)
    non-real: bright purple (like the text color)
    NaN: black
    over 24: bright cyan
    over 9000: uhhhhhhhhhh idk honestly maybe just delete this one because it never really gets used anyway LOL

the HTML "type custom name" box should hide if you get booted out of the select team screen (battle master leaves)

all team names should be erased from everyone's memory if the battle master ends the battle or leaves

make the JOIN battle button more obvious. maybe flash red or has particle effects even

make this https://entropychicken.github.io/24game/ redirect to make24.org (because of people who are still on the old url)
make this https://entropychicken.github.io/make-24/ redirect to make24.org and have this be the official github name

make the buttons look nicer on the home screen lol. don't be on one and two lines for the duel and battle buttons on mobile, for example
make the text sizes of the buttons consistent in the Level

add hint for "cards":[-1,-1],"ops":["÷","√","∛","ln"]

investigate if this block of code from battleSystem.js is too lax for making isOnlineSession and may cause bugs
    await channel.subscribe((status) => {
        if (status === "SUBSCRIBED") {
            isOnlineSession = true; // sketchy?

make numberInput only accept decimal real numbers (maybe don't even let people type PI) (clarify no imaginary numbers)
make consistent between this.numberInput versus. teamInput

maybe red background means the user's puzzle has not been verified, while blue background means it has been verified

make the auto gen hint for custom levels depend on language